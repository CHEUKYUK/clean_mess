1、images包中包含test，train，val三个数据包
2、程序在jupyter notebook中运行
3、运行前根据注释提示将读取数据部分改成数据包所在路径
4、程序输出每次训练所得精确度与误差值，且包含可视化结果
5、程序输出对测试集的测试结果，0表示“整洁”，1表示“杂乱”

1.The images package contains three data packets: test, train, and val.
2.The program runs in a jupyter notebook
3.Before running, change the read data part to the path of the data packet according to the comment prompt.
4.Outputs: the accuracy and error values obtained from each training, and includes visualization results
5.Results: 0 means "clean" and 1 means "messy"
